# --- ML / Replicate ---
replicate==0.34.0

# --- Web ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
jinja2==3.1.4

# --- Storage (Yandex Object Storage, S3-compatible) ---
boto3==1.35.0

# --- Payments ---
# Робокасса работает по простому HTTP/HMAC — отдельный SDK не нужен.
# Для перехода на ЮКассу позже: yookassa==3.5.0

# --- Email ---
# stdlib smtplib — внешних зависимостей нет

# --- Utils ---
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2
Pillow==10.4.0
